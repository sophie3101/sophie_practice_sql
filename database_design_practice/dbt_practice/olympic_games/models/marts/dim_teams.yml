version: 2

models:
  - name: dim_teams
    columns:
      - name: team_id
        tests: [unique, not_null]
      - name: team 
        tests:  
          - not_null
      - name: noc 
        tests:  
          - not_null